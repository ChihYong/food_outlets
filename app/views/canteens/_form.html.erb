<%= form_for(@canteen) do |f| %>
  <% if @canteen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@canteen.errors.count, "error") %> prohibited this canteen from being saved:</h2>

      <ul>
      <% @canteen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_area :location, :rows => "3" %>
  </div>
  <div class="field">
    <%= f.label :no_of_stalls %><br />
    <%= f.text_field :no_of_stalls %>
  </div>
  <div class="field">
    <%= f.label :operating_hours %><br />
    <%= f.text_area :operating_hours, :rows => "5" %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :announcement %><br />
    <%= f.text_area :announcement, :rows => "5" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
